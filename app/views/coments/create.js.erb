$('.add-coment-errors').html('<%= render 'shared/errors', resource: @coment %>');

<% if @coment.persisted? %>
  $('.coments').append('<%= j render @answer %>');
  $('.new-answer #answer_body').val('');
<% end %>
