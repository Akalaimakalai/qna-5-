h1= @question.title
p= @question.body
p= link_to 'Delete question', question_path(@question), method: :delete if user_is_author?

ul= render @question.answers

p= render 'answers/form' if user_signed_in?
