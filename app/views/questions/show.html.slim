h1= @question.title
p= @question.body
p= link_to 'Delete question', question_path(@question), method: :delete if current_user&.is_author?(@question)

.answers
  = render @question.answers


= render 'answers/form' if user_signed_in?
