$('.update-question-errors').html('<%= render 'shared/errors', resource: @question %>');

<% unless @question.errors.present? %>
  $('<%= "#question-id-#{@question.id}" %>').replaceWith('<%= j render 'questions/show_question', question: @question %>');
<% end %>
