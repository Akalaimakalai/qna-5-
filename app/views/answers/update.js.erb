$('.update-answer-errors').html('<%= render 'shared/errors', resource: @answer %>');

<% if @answer.errors.empty? %>
  $('<%= "#answer-id-#{@answer.id}" %>').replaceWith('<%= j render @answer %>');
<% end %>
